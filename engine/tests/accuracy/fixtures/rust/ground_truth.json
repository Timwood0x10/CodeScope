{
  "language": "rust",
  "expected_calls": [
    {"caller": "main_func", "caller_file": "a.rs", "callee": "alpha", "callee_file": "a.rs"},
    {"caller": "alpha", "caller_file": "a.rs", "callee": "bravo", "callee_file": "b.rs"}
  ],
  "forbidden_calls": [
    {"caller": "main_func", "callee": "len"},
    {"caller": "main_func", "callee": "vec"},
    {"caller": "main_func", "callee": "helper", "note": "b.rs:helper is a free function, NOT the method c.helper()"}
  ],
  "allowed_unresolved": [
    {"caller": "main_func", "callee": "new", "note": "Container::new() — associated function; receiver_type=Container, resolver not yet using it"},
    {"caller": "main_func", "callee": "get", "note": "c.get() — method call; receiver_type=Container, resolver not yet using it"},
    {"caller": "main_func", "callee": "helper", "note": "c.helper() — method call; receiver_type=Container, resolver not yet using it"},
    {"caller": "helper", "caller_file": "a.rs", "callee": "get", "note": "self.get() — receiver_type=Container, resolver not yet using it"}
  ],
  "external_calls": ["vec!", "len"]
}
