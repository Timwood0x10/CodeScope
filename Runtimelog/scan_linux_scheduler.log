# Linux Kernel Process Scheduling & Parent-Child Resource Management
## CodeScope 自动化分析报告

**日期**: 2026-07-04
**分析工具**: CodeScope Engine + codebase-memory-mcp
**源码位置**: /tmp/linux-src/
**分析文件**:
  - kernel/sched/core.c (11,284 行) — 核心调度器
  - kernel/fork.c (3,409 行) — 进程创建与资源复制
  - kernel/exit.c (1,985 行) — 进程终止与资源回收

---

## 1. 进程调度机制 (Process Scheduling)

### 1.1 总体架构

Linux 调度器采用**模块化的调度类 (Scheduler Class)** 架构，按优先级排列：

```
停止类 (stop_sched_class)      → 最高优先级，用于热插拔/停止
限期类 (dl_sched_class)        → Deadline scheduling (SCHED_DEADLINE)
实时类 (rt_sched_class)        → 实时进程 (SCHED_FIFO/SCHED_RR)
公平类 (fair_sched_class)      → CFS 完全公平调度 (SCHED_NORMAL/SCHED_BATCH)
空闲类 (idle_sched_class)      → 空闲线程 (per-CPU idle)
```

选择策略：`__pick_next_task()` (core.c:6124) 按上述顺序轮询，高优先级类有任务则优先。

### 1.2 调度入口: `schedule()` (core.c:7316)

```c
asmlinkage __visible void __sched schedule(void)
{
    struct task_struct *tsk = current;
    if (!task_is_running(tsk))
        sched_submit_work(tsk);   // 提交未完成的 IO 工作
    __schedule_loop(SM_NONE);      // 核心调度循环
    sched_update_worker(tsk);      // 更新 kworker 状态
}
```

`schedule()` 是内核中主动调用的入口。任何需要让出 CPU 的代码路径（如 `wait_event`、`mutex_lock`、`cond_resched()` 等）最终都调用此函数。

### 1.3 核心调度: `__schedule()` (core.c ~570 行附近)

核心调度循环的三步曲：

```
__schedule():
  ┌─ 1. 选择下一个任务 ─────────────────────────────┐
  │   next = pick_next_task(rq, &rf);               │
  │   遍历调度类层次: stop → dl → rt → fair → idle  │
  └──────────────────────────────────────────────────┘
  ┌─ 2. 上下文切换 ─────────────────────────────────┐
  │   if (prev != next)                             │
  │       context_switch(rq, prev, next, rf);       │
  └──────────────────────────────────────────────────┘
  ┌─ 3. 后续清理 (在 finish_task_switch 中) ───────┐
  │   释放 prev->mm (延迟释放)                     │
  │   处理 TASK_DEAD 状态                          │
  └──────────────────────────────────────────────────┘
```

### 1.4 任务选择: `pick_next_task()` → `__pick_next_task()` (core.c:6124-6216)

核心选择逻辑：
```c
static inline struct task_struct *
__pick_next_task(struct rq *rq, struct rq_flags *rf)
{
    const struct sched_class *class;
    struct task_struct *p;

    // 优化路径: 如果 runqueue 只有 CFS 任务，直接选 fair 类
    if (likely(!dl_task(rq->curr) && !rt_task(rq->curr) &&
               rq->nr_running == rq->cfs.nr_running)) {
        p = pick_next_task_fair(rq, NULL, rf);
        if (likely(p))
            return p;
    }

    // 完整路径: 遍历调度类层次
    for_each_active_class(class) {
        p = class->pick_next_task(rq);
        if (p)
            return p;
    }

    BUG(); // 至少 idle 类应返回任务
}
```

优化路径处理常见情况（仅 CFS 任务），避免不必要的遍历。

### 1.5 上下文切换: `context_switch()` (core.c:5451)

```c
static __always_inline struct rq *
context_switch(struct rq *rq, struct task_struct *prev,
               struct task_struct *next, struct rq_flags *rf)
{
    prepare_task_switch(rq, prev, next);    // 调度器类准备钩子

    // 切换内存上下文 (MM)
    if (!next->mm) {                        // 切换到内核线程
        enter_lazy_tlb(prev->active_mm, next);
        next->active_mm = prev->active_mm;
        // ... 引用计数管理
    } else {                                // 切换到用户态进程
        switch_mm(rq, prev->active_mm, next->mm);
        // ... membarrier 同步屏障
    }

    // 切换寄存器状态
    switch_to(prev, next, prev);
    // ★ 执行到这里时，CPU 已经运行在 next 的栈上 ★

    // finish_task_switch() 在切回来之后执行清理
    barrier();
    return finish_task_switch(prev);
}
```

关键设计：
- **Lazy TLB**: 内核线程不拥有自己的 `mm`，复用上一个用户进程的 `active_mm`
- **`switch_to()` 是架构相关的宏** (x86_64 的 `__switch_to_asm` + `__switch_to`)
- **控制流魔法**: `switch_to()` 可以从 A 切到 B，但稍后从 C 切回 A 时，`finish_task_switch()` 在 A 的上下文中继续执行

### 1.6 finish_task_switch() — 切换后清理 (core.c:5318)

```c
static struct rq *finish_task_switch(struct task_struct *prev)
{
    struct rq *rq = this_rq();
    struct mm_struct *mm = rq->prev_mm;
    rq->prev_mm = NULL;

    // 延迟释放 prev 进程的 mm（避免在持有 rq->lock 时释放）
    if (mm)
        mmdrop(mm);

    // 检查 prev 是否已死亡 (TASK_DEAD)
    if (prev->state == TASK_DEAD) {
        // 释放 task_struct 的最后一个引用
        put_task_struct(prev);
    }
    return rq;
}
```

---

## 2. 父子进程资源管理 (Parent-Child Resource Management)

### 2.1 进程创建: `copy_process()` (fork.c:1994)

`fork()/clone()` 系统调用最终调用 `copy_process()`，按严格顺序复制或共享资源：

```
copy_process():
  │
  ├─ 1. dup_task_struct()       ── 复制 task_struct（进程描述符）
  │     ├─ arch_dup_task_struct() ── 架构相关的结构复制
  │     └─ alloc_thread_info_node() ── 分配内核栈
  │
  ├─ 2. 复制/共享标志检查        ── 检查 CLONE_* 标志合法性
  │
  ├─ 3. 资源复制（按序）:
  │     ├─ copy_semundo()        ── SYSV 信号量撤消 (CLONE_SYSVSEM)
  │     ├─ copy_files()          ── 文件描述符表 (CLONE_FILES)
  │     ├─ copy_fs()             ── 文件系统上下文 (CLONE_FS)
  │     ├─ copy_sighand()        ── 信号处理函数 (CLONE_SIGHAND)
  │     ├─ copy_signal()         ── 信号统计结构
  │     ├─ copy_mm()             ── 内存描述符 (CLONE_VM)
  │     ├─ copy_namespaces()     ── 命名空间 (CLONE_NEWNS|NEW*)
  │     ├─ copy_io()             ── IO 上下文
  │     ├─ copy_thread()         ── 寄存器状态 + 内核栈布局
  │     └─ sched_fork()          ── 调度实体初始化
  │
  ├─ 4. 插入调度队列
  │     └─ wake_up_new_task(p)   ── 子进程首次加入 runqueue
  │
  └─ 5. 返回 p (子进程的 task_struct)
```

### 2.2 关键资源复制策略

**COW (Copy-on-Write) — `copy_mm()` (fork.c:1568)**

```c
static int copy_mm(u64 clone_flags, struct task_struct *tsk)
{
    struct mm_struct *mm, *oldmm = current->mm;

    if (clone_flags & CLONE_VM) {
        // 共享地址空间（线程）: 增加引用计数
        mmget(oldmm);
        mm = oldmm;
        goto good_mm;
    }

    // 新进程: 创建 mm 并设置 COW
    mm = dup_mm(tsk, current->mm);
    // dup_mm() → dup_mmap() → 遍历 VMA，设置页表为只读
    // 首次写访问时触发 page fault，复制物理页
good_mm:
    tsk->mm = mm;
    tsk->active_mm = mm;
    return 0;
}
```

| 标志 | 行为 | 适用场景 |
|------|------|----------|
| `CLONE_VM` | 共享地址空间（引用计数） | 线程 (pthread) |
| 无 `CLONE_VM` | Copy-on-Write | fork() 子进程 |

**文件描述符 — `copy_files()` (fork.c:1642)**
- `CLONE_FILES`: 共享 fd 表（线程模型）
- 无 `CLONE_FILES`: 复制整个 fd_table，`dup_fd()` 深拷贝

**文件系统 — `copy_fs()` (fork.c:1621)**
- `CLONE_FS`: 共享 cwd/root（线程模型）
- 无 `CLONE_FS`: `copy_fs_struct()` 复制 fs_struct

**信号 — `copy_sighand()` + `copy_signal()` (fork.c:1672+1721)**
- `CLONE_SIGHAND`: 共享信号处理函数表
- `copy_signal()`: 始终复制信号统计结构（不共享），用于 `wait()` 统计

### 2.3 进程终止 — `do_exit()` / `exit_mm()` / `exit_notify()` (exit.c)

```
do_exit():
  │
  ├─ 1. exit_signals()          ── 清理待处理信号
  ├─ 2. exit_mm()               ── 释放内存
  │     ├─ mm_release()          ── 释放用户空间状态
  │     ├─ exit_mm_release()     ── 调度 mm 释放
  │     └─ mmput() → mmdrop()   ── 减少引用计数，归零时真正释放
  │
  ├─ 3. 释放各类资源:
  │     ├─ exit_sem()            ── SYSV 信号量
  │     ├─ __exit_signal()       ── 信号结构清理
  │     ├─ exit_files()          ── 文件描述符
  │     ├─ exit_fs()             ── 文件系统上下文
  │     └─ exit_namespace()      ── 命名空间
  │
  ├─ 4. exit_notify()           ── 通知父进程
  │     ├─ forget_original_parent() ── 子进程重新托管
  │     │   ├─ 找到可用的 "reaper" 进程（当前进程的线程组 leader、init 进程）
  │     │   └─ 将子进程重新挂载到 reaper 下
  │     ├─ do_notify_parent()    ── 发送 SIGCHLD 给父进程
  │     └─ TASK_DEAD 状态设置
  │
  ├─ 5. schedule()              ── 最后一次调度（永不返回）
  │
  └─ 6. (在 finish_task_switch 中)
        └─ put_task_struct()    ── 释放 task_struct（父进程 wait() 后）
```

### 2.4 资源回收 — `wait()` / `release_task()` (exit.c:244)

```
父进程调用 wait4()/waitpid():
  │
  ├─ 收集子进程退出状态
  ├─ release_task():
  │     ├─ __exit_signal()      ── 释放 signal_struct 引用
  │     ├─ __unhash_process()   ── 从 pidhash 和线程链表移除
  │     ├─ do_notify_parent()   ── 如果还未通知，发 SIGCHLD
  │     └─ put_task_struct()    ── 释放 task_struct 内存
  │
  └─ 返回 exit_code 给父进程
```

**孤儿进程处理**: 如果父进程先于子进程退出，`exit_notify()` 中的 `forget_original_parent()` 将子进程重新托管给：
1. 当前线程组的 leader（同一进程内的其他线程）
2. init 进程 (PID 1) — 最后的保障

**僵尸进程 (Zombie)**: 子进程已退出但父进程未 `wait()`。此时：
- task_struct 保留（占约 1-2KB，包含退出状态）
- 所有其他资源（mm、files、fs 等）已释放
- `release_task()` 在父进程 `wait()` 后才最终释放 task_struct

---

## 3. 关键设计模式总结

| 机制 | 核心原理 | 关键函数/文件 |
|------|---------|-------------|
| **调度类层次** | stop → dl → rt → fair → idle 优先级链 | `__pick_next_task()` (core.c:6124) |
| **时间片管理** | CFS: 虚拟运行时间 (vruntime)，红黑树选择最小 | `pick_next_task_fair()` (fair.c) |
| **上下文切换** | MM 切换 + 寄存器切换 + lazy TLB | `context_switch()` (core.c:5451) |
| **COW 内存** | fork() 后页表只读，写时复制 | `copy_mm()` → `dup_mmap()` (fork.c) |
| **CLONE 标志** | 位掩码控制资源共享粒度 | `copy_process()` (fork.c:1994) |
| **wait 同步** | 父进程必须 wait() 回收 task_struct | `release_task()` (exit.c:244) |
| **孤儿托管** | 子进程重新挂载到 init (PID 1) | `forget_original_parent()` (exit.c) |
| **僵尸预防** | 及时 wait() 避免 task_struct 泄漏 | `do_notify_parent()` (exit.c) |

---

## 4. Benchmark 性能数据

使用 `test_bench` 对 `/tmp/linux-src/kernel/latencytop.c` (7.6 KB) 的测量结果：

| 指标 | 值 |
|------|-----|
| 引擎初始化 | 14.28 ms |
| 索引时间 | 4.94 ms |
| 索引吞吐量 | 1535.4 KB/s |
| 查询时间 (9 次) | 0.17 ms (总计) |
| 定义查询 (单次) | 0.01-0.03 ms |
| 引用查询 (单次) | 0.02-0.03 ms |
| 调用者查询 (单次) | 0.02 ms |
| 被调用者查询 (单次) | 0.01-0.02 ms |
| SQLite 扩展支持 | ✅ sqlite-vec loaded |
| SQLite DB 大小 | ~230 KB (包含完整 schema) |

**Token 节省分析**:
- 源码大小: 7,763 字符
- 索引 JSON: 33 字符 (节省 99.6%)
- 查询结果: 24-51 字符/次
- 典型替代方案: 直接传源码给 LLM 每次约 1,500-2,000 tokens
- CodeScope 方案: 30-80 tokens/次查询

---

*分析完成时间: 2026-07-04 19:58 UTC*
*使用 CodeScope Engine + codebase-memory-mcp 自动化完成*
